naftiko: 1.0.0-alpha2
info:
  label: Sonatype Nexus Repository Manager — Routing rules
  description: 'Sonatype Nexus Repository Manager — Routing rules. 5 operations. Lead operation: List routing rules. Self-contained
    Naftiko capability covering one Sonatype Nexus business surface.'
  tags:
  - Sonatype Nexus
  - Routing rules
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SONATYPE_NEXUS_API_KEY: SONATYPE_NEXUS_API_KEY
capability:
  consumes:
  - type: http
    namespace: repository-routing-rules
    baseUri: ''
    description: Sonatype Nexus Repository Manager — Routing rules business capability. Self-contained, no shared references.
    resources:
    - name: v1-routing-rules
      path: /v1/routing-rules
      operations:
      - name: getroutingrules
        method: GET
        description: List routing rules
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: createroutingrule
        method: POST
        description: Create a single routing rule
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: v1-routing-rules-name
      path: /v1/routing-rules/{name}
      operations:
      - name: deleteroutingrule
        method: DELETE
        description: Delete a single routing rule
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: name
          in: path
          type: string
          description: The name of the routing rule to delete
          required: true
      - name: getroutingrule
        method: GET
        description: Get a single routing rule
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: name
          in: path
          type: string
          description: The name of the routing rule to get
          required: true
      - name: updateroutingrule
        method: PUT
        description: Update a single routing rule
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: name
          in: path
          type: string
          description: The name of the routing rule to update
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: basic
      username: '{{env.SONATYPE_NEXUS_USER}}'
      password: '{{env.SONATYPE_NEXUS_PASS}}'
  exposes:
  - type: rest
    namespace: repository-routing-rules-rest
    port: 8080
    description: REST adapter for Sonatype Nexus Repository Manager — Routing rules. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/v1/routing-rules
      name: v1-routing-rules
      description: REST surface for v1-routing-rules.
      operations:
      - method: GET
        name: getroutingrules
        description: List routing rules
        call: repository-routing-rules.getroutingrules
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createroutingrule
        description: Create a single routing rule
        call: repository-routing-rules.createroutingrule
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/v1/routing-rules/{name}
      name: v1-routing-rules-name
      description: REST surface for v1-routing-rules-name.
      operations:
      - method: DELETE
        name: deleteroutingrule
        description: Delete a single routing rule
        call: repository-routing-rules.deleteroutingrule
        with:
          name: rest.name
        outputParameters:
        - type: object
          mapping: $.
      - method: GET
        name: getroutingrule
        description: Get a single routing rule
        call: repository-routing-rules.getroutingrule
        with:
          name: rest.name
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: updateroutingrule
        description: Update a single routing rule
        call: repository-routing-rules.updateroutingrule
        with:
          name: rest.name
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: repository-routing-rules-mcp
    port: 9090
    transport: http
    description: MCP adapter for Sonatype Nexus Repository Manager — Routing rules. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: list-routing-rules
      description: List routing rules
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: repository-routing-rules.getroutingrules
      outputParameters:
      - type: object
        mapping: $.
    - name: create-single-routing-rule
      description: Create a single routing rule
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: repository-routing-rules.createroutingrule
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: delete-single-routing-rule
      description: Delete a single routing rule
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: repository-routing-rules.deleteroutingrule
      with:
        name: tools.name
      outputParameters:
      - type: object
        mapping: $.
    - name: get-single-routing-rule
      description: Get a single routing rule
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: repository-routing-rules.getroutingrule
      with:
        name: tools.name
      outputParameters:
      - type: object
        mapping: $.
    - name: update-single-routing-rule
      description: Update a single routing rule
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: repository-routing-rules.updateroutingrule
      with:
        name: tools.name
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
