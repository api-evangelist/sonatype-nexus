naftiko: 1.0.0-alpha2
info:
  label: 'Sonatype Nexus Repository Manager — Security management: api keys principals encryption'
  description: 'Sonatype Nexus Repository Manager — Security management: api keys principals encryption. 1 operations. Lead
    operation: Re-encrypt api keys principals using the specified configuration. Self-contained Naftiko capability covering
    one Sonatype Nexus business surface.'
  tags:
  - Sonatype Nexus
  - 'Security management: api keys principals encryption'
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SONATYPE_NEXUS_API_KEY: SONATYPE_NEXUS_API_KEY
capability:
  consumes:
  - type: http
    namespace: repository-security-management-api-keys-principals-encryption
    baseUri: ''
    description: 'Sonatype Nexus Repository Manager — Security management: api keys principals encryption business capability.
      Self-contained, no shared references.'
    resources:
    - name: v1-apikeys-encryption-re-encrypt
      path: /v1/apikeys/encryption/re-encrypt
      operations:
      - name: reencrypt
        method: PUT
        description: Re-encrypt api keys principals using the specified configuration
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: basic
      username: '{{env.SONATYPE_NEXUS_USER}}'
      password: '{{env.SONATYPE_NEXUS_PASS}}'
  exposes:
  - type: rest
    namespace: repository-security-management-api-keys-principals-encryption-rest
    port: 8080
    description: 'REST adapter for Sonatype Nexus Repository Manager — Security management: api keys principals encryption.
      One Spectral-compliant resource per consumed operation, prefixed with /v1.'
    resources:
    - path: /v1/v1/apikeys/encryption/re-encrypt
      name: v1-apikeys-encryption-re-encrypt
      description: REST surface for v1-apikeys-encryption-re-encrypt.
      operations:
      - method: PUT
        name: reencrypt
        description: Re-encrypt api keys principals using the specified configuration
        call: repository-security-management-api-keys-principals-encryption.reencrypt
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: repository-security-management-api-keys-principals-encryption-mcp
    port: 9090
    transport: http
    description: 'MCP adapter for Sonatype Nexus Repository Manager — Security management: api keys principals encryption.
      One tool per consumed operation, routed inline through this capability''s consumes block.'
    tools:
    - name: re-encrypt-api-keys-principals
      description: Re-encrypt api keys principals using the specified configuration
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: repository-security-management-api-keys-principals-encryption.reencrypt
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
